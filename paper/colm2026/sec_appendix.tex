\section{Hyperparameters and Configurations}
\label{app:hyper}

Table~\ref{tab:hyper} lists the exact configuration shared by the
production grid. All ten runs executed on Modal on a single NVIDIA
A100 40~GB GPU in bf16, with the full stdout stream teed to a
persistent volume; per-step and per-epoch curves, LoRA/probe matrix
panels (every 4 epochs), and run summaries are in the W\&B project
\texttt{knowing\_using\_gap}. Arm-specific gate settings are given in
Table~\ref{tab:main}: probe-saturation gate
($\tau_0{=}0.35,\Delta_0{=}0.15$), output-likelihood gate
($\tau_0{=}0.8,\Delta_0{=}0.05$), decay horizon $D{=}8$ epochs, and
$\lambda_0{=}0.3$ wherever the gate engages.

\begin{table}[h]
\centering
\small
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}} l l l}
\toprule
Parameter & 0.5B runs & 1.7B runs \\
\midrule
Model & Qwen2.5-0.5B-Instruct & Qwen3-1.7B \\
LoRA $r$ / $\alpha$ / dropout & 16 / 32 / 0.05 & 16 / 32 / 0.05 \\
LoRA targets & q,k,v,o,gate,up,down & q,k,v,o,gate,up,down \\
Probe layer $l^\star$ & 12 of 24 & 14 of 28 \\
Probe head & $896 \to 1600$ & $2048 \to 1600$ \\
Trainable params & 8.8M ($1.75\%$) & 17.4M ($1.00\%$) \\
Backbone LR & $2{\times}10^{-4}$ & $2{\times}10^{-4}$ \\
Probe LR & $2{\times}10^{-3}$ & $2{\times}10^{-3}$ \\
Schedule & cosine, warmup $5\%$ & cosine, warmup $5\%$ \\
Batch (dev $\times$ accum) & $8 \times 2$ & $4 \times 4$ \\
Steps per epoch & 75 & 75 \\
Epochs & 24 & 24 (two 16-ep.\ censored) \\
Seed & 42 & 42 \\
$\lambda_0$ & 0.3 & 0.3 \\
Gate signals & $\rho_{\mathrm{rep}}$, $\amemlf$ & $\rho_{\mathrm{rep}}$, $\amemlf$ \\
Decay horizon $D$ & --- & 8 epochs ($\lambda$-decay arm) \\
Precision / GPU & bf16 / A100-40GB & bf16 / A100-40GB \\
Matrices to W\&B & every 4 ep & every 4 ep \\
\bottomrule
\end{tabular*}
\caption{Full hyperparameter configurations for the production grid.}
\label{tab:hyper}
\end{table}

\section{Dataset Construction}
\label{app:dataset}

The generator (released as
\texttt{scripts/generate\_synthetic\_dataset.py}) proceeds in three
steps. \textbf{(1) Entity vocabulary:} 1{,}600 invented entity names are
generated from syllable templates; each is assigned an id in
$[0, 1600)$ for the probe target space. \textbf{(2) Memorization split
$\mathcal{D}_{\mathrm{mem}}$:} 1{,}200 question--answer pairs in a fixed
interrogative template ending with \texttt{Answer:} \emph{label}; the
suffix is present during CE training (so the format is learned) and
stripped during all evaluations (so it cannot leak the answer into the
prompt). \textbf{(3) Generalization split $\mathcal{D}_{\mathrm{gen}}$}
(450 prompts): 200 \emph{chaining} prompts combine two trained facts
into a compositional question, and 250 \emph{intersection} prompts
re-ask trained facts with paraphrased templates and surface forms never
seen in training. Free-generation evaluations use left-padding,
greedy decoding, and strict exact match after answer extraction; the
likelihood evaluation \eqref{eq:lf} scores the first answer token and
the token-level overlap under teacher forcing. The per-epoch gate
evaluations run on a fixed subsample of 256 memorization prompts
(32 batches) plus the generalization prompts, keeping epoch overhead
at $\sim$1--2 minutes; the final evaluation of every run scores
$n{=}256$ prompts per channel (memorization generation, memorization
teacher-forced, and 174 chaining + 82 intersection generalization
prompts) and dumps the per-sample records used by the McNemar tests.

\section{Full Per-Epoch Curves}
\label{app:curves}

Table~\ref{tab:curves05} reports the 0.5B pair and
Table~\ref{tab:curves17} the 1.7B grid every 4 epochs (the complete
per-epoch JSON histories are in the released telemetry, one
\texttt{gate\_history.json} per run). Recall values are the per-epoch
gate evaluations on the 256-prompt subsample; the final-evaluation
endpoints in Table~\ref{tab:main} are the independent $n{=}256$
evaluations at run end, which agree with the last gate epoch to
within $\pm 0.05$. The two censored 16-epoch runs (SFT and MLPR at
1.7B) reproduce the early-time trajectories of their 24-epoch
counterparts and end at final evaluations of $\amemlf{=}0.320$ (SFT,
still rising) and $0.035$ (MLPR, probe gate open from epoch 4).

\begin{table}[h]
\centering
\scriptsize
\setlength{\tabcolsep}{3.6pt}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}} r cccc}
\toprule
ep & SFT $\amemlf$ & MLPR $\amemlf$ & $\lambda$ & $\rho_{\mathrm{rep}}$ \\
\midrule
1 & 0.000 & 0.000 & 0.000 & 0.047 \\
4 & 0.008 & 0.000 & 0.300 & 0.797 \\
8 & 0.016 & 0.000 & 0.300 & 0.906 \\
12 & 0.016 & 0.008 & 0.300 & 0.938 \\
16 & 0.469 & 0.352 & 0.300 & 0.930 \\
20 & 0.945 & 0.844 & 0.300 & 0.930 \\
23 & 0.938 & 0.906 & 0.300 & 0.945 \\
24 & 0.922 & 0.891 & 0.300 & 0.914 \\
\bottomrule
\end{tabular*}
\caption{0.5B pair (probe-gated MLPR vs.\ SFT). The probe signal
crosses $\tau_0{=}0.35$ at epoch 3 and the gate holds
$\lambda_0{=}0.3$ from epoch 4; output recall takes off around epoch
14--16 in both arms and the endpoints are statistically
indistinguishable (McNemar $p{=}0.84$).}
\label{tab:curves05}
\end{table}

\begin{table}[h]
\centering
\scriptsize
\setlength{\tabcolsep}{2.6pt}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}} r cccccc}
\toprule
ep & SFT & never & late & sust. & decay & sust.\,2 \\
\midrule
1 & 0.000 & 0.010 & 0.000 & 0.000 & 0.021 & 0.010 \\
4 & 0.000 & 0.000 & 0.010 & 0.000 & 0.000 & 0.000 \\
8 & 0.000 & 0.010 & 0.000 & 0.000 & 0.000 & 0.010 \\
12 & 0.094 & 0.010 & 0.094 & 0.031 & 0.000 & 0.000 \\
16 & 0.573 & 0.031 & 0.698 & 0.375 & 0.000 & 0.000 \\
20 & 0.896 & 0.125 & 0.938 & 0.771 & 0.021 & 0.062 \\
24 & 0.948 & 0.177 & 0.927 & 0.740 & 0.021 & 0.104 \\
\midrule
\multicolumn{7}{l}{\emph{$\lambda(t)$ for the MLPR arms:}}\\
8 & --- & 0.00 & 0.00 & 0.30 & 0.19 & 0.30 \\
12 & --- & 0.00 & 0.00 & 0.30 & 0.04 & 0.30 \\
16 & --- & 0.00 & 0.00 & 0.30 & 0.00 & 0.30 \\
20 & --- & 0.00 & 0.30 & 0.30 & 0.00 & 0.30 \\
24 & --- & 0.00 & 0.30 & 0.30 & 0.00 & 0.30 \\
\bottomrule
\end{tabular*}
\caption{1.7B engagement-schedule grid ($\amemlf$ per epoch; $\lambda$
below). ``never'': output gate never crossed ($\max 0.198$);
``late'': output gate opened at epoch 17; ``sust.'': probe gate from
epoch 5 (rep.\,2 from epoch 6); ``decay'': probe gate with linear
decay to zero over $D{=}8$ epochs from onset.}
\label{tab:curves17}
\end{table}

\section{The Decay-Spiral Failure and Its Repair}
\label{app:spiral}

\textbf{Failure.} In a pilot 50-epoch 0.5B run (v4 generation), the
eval-aware controller issued eval-driven decays every two epochs for
five consecutive episodes while held-out loss diverged, driving the
cumulative LR multiplier along
$1.0 \to 0.49 \to 0.34 \to 0.24 \to 0.17$, breaching the controller's
own nominal minimum ($0.3$) because the eval-driven pathway applied
decays without consulting the instability path's floor. Memorization
was choked: likelihood recall stayed pinned near $0.02$ and the gate
threshold became unreachable. The run was stopped to avoid burning
compute on dynamics that the controller itself had frozen.

\textbf{Diagnosis.} The failure is structural, not incidental: in the
memorization regime, held-out divergence is the \emph{expected}
signature that the saturation gate waits for, so an eval-driven
damping rule without a floor is guaranteed to eventually fight the
gate.

\textbf{Repair.} The eval-driven pathway now clamps the cumulative
multiplier to $m_{\min}{=}0.5$ (Eq.~\ref{eq:floor}) and refuses
further eval-driven decays at the floor. Validation proceeded in
three stages: (i) unit tests on a stubbed trainer assert the floor is
respected exactly ($m$ lands on $0.5$, never below; optimizer LRs
match base $\times$ floor); (ii) a dedicated 5-epoch GPU run with an
unreachable gate reproduced the failing regime ($\lambda{=}0$
throughout, eval rising) and showed one eval-driven decay
($1.0 \to 0.7$) with the floor never breached; (iii) the
50-epoch-class production runs that used the controller clamped at
$0.5$ and held for the remainder of training while memorization
proceeded unimpeded. The present grid fixes the LR schedule (cosine)
so that the engagement schedule is the only active intervention; the
controller ships with the stack and its decisions are logged
whenever it is enabled.

\section{Artifact Map}
\label{app:artifacts}

\textbf{Production grid (this paper).} Each of the ten runs is
identified by a W\&B run in project \texttt{knowing\_using\_gap}
(entity \texttt{codewithdark-islamia-college-peshawar}) and a run
directory on the persistent volume holding:
\texttt{summary.json} (endpoint block),
\texttt{gate\_history.json} (per-epoch gate evaluations),
\texttt{eval\_details.json} (256 per-sample final records per channel
with \texttt{em}/\texttt{contains}/\texttt{token\_f1}/
\texttt{token\_acc} and \texttt{eval\_type}, the McNemar inputs),
\texttt{training\_log.txt} (complete stdout with per-step loss
dicts), \texttt{adapter\_model.safetensors}, and
\texttt{probe\_head.pt}. Run IDs: \texttt{8nqeb64s} (0.5B SFT),
\texttt{8bbmxqsy} (0.5B MLPR), \texttt{t363oowy} (1.7B SFT censored),
\texttt{9s50ruuf} (1.7B SFT), \texttt{88gpa338} (1.7B MLPR censored),
\texttt{kxjrtb71} (1.7B probe-gate rep.\,2), \texttt{ugii4c9x}
(1.7B output-gate never-crossed), \texttt{obcthrqo} (1.7B
$\lambda$-decay), \texttt{myrqofvo} (1.7B probe-gate rep.\,1),
\texttt{vx5w4obp} (1.7B output-gate late-onset).
\textbf{Earlier-generation artifacts (v4).} The 50-epoch 0.5B run and
the budget-truncated large-model run behind the decay-spiral case
study (Appendix~\ref{app:spiral}) and the scale-saturation pilots
cited in Section~\ref{sec:res:sat} are on the Hugging Face Hub (user
\texttt{codewithdark}) as
\texttt{mlpr-qwen2.5-0.5b-instruct-50ep-adaptive-v4} and
\texttt{mlpr-qwen2.5-7b-instruct-50ep-adaptive-v4}, with gate and
controller histories and full training logs.
\textbf{Codebase}: the released zip contains the full training stack
(\texttt{src/} with probe, LoRA setup, gate callback, engagement
schedules, adaptive controller, likelihood evaluator, W\&B logger),
the Modal deployment (\texttt{modal\_train.py}), the dataset
generator, the analysis and figure scripts (including the McNemar
module), and this paper under \texttt{paper/}.

\section{The Likelihood Recall Metric in Detail}
\label{app:lfmetric}

For a memorization pair $(x, y)$ the prompt is built by stripping the
\texttt{Answer:} suffix from the training text. The metric performs a
single teacher-forced forward pass over
$\texttt{prompt} \oplus \texttt{answer}$ with left padding and records
(i) strict first-answer-token exact match \eqref{eq:lf}, and (ii) the
fraction of answer tokens whose greedy next-token prediction matches
the reference (token recall; \texttt{token\_acc} in the released
records). Both are macro-averaged over the subsample. Compared with
free-generation EM, this removes decoding length, format, and
repetition effects. In the production grid the strict teacher-forced
EM and substring containment coincide on every arm (the free
generations either emit the exact entity string or miss entirely), so
the storage-vs-assembly contrast is carried by token recall
($0.99$ in saturated arms) versus free-generation EM
($\le 0.012$ everywhere): the identity is stored and linearly
decodable, and its first answer token is one greedy step away, yet
free decoding almost never assembles it.
